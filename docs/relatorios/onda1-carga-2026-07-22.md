# Onda 1 — teste de carga (5 mil processos)

Data: 2026-07-22  
Responsável: Engenharia

## Resultado

**Bloqueado por ambiente.** O script `scripts/onda1/load-test.mjs` está pronto e recusa
explicitamente o projeto Supabase atual. Para executar:

```bash
DATABASE_URL_TEST=postgresql://... \
LOAD_TEST_CONFIRM=ISOLATED_DATABASE \
LOAD_TEST_PROCESSOS=5000 \
npm run test:load
```

## Metas (p95)

| Consulta | Limite |
|---|---|
| Listagem paginada (25) | ≤ 200 ms |
| Busca CNJ | ≤ 500 ms |
| Busca parcial nome | ≤ 800 ms |
| Counts do dashboard | ≤ 1000 ms |

## Pré-requisitos já aplicados no projeto

- Índices em `processos`, `clientes` (pg_trgm) e `verificacoes_datajud`
- Coluna `proxima_verificacao_em` + cron Datajud em lotes horários (`vercel.json`)
- Limpeza automática dos dados sintéticos no `finally` do script

## Próximo passo

Provisionar banco descartável, rodar o script e anexar o JSON gerado em
`docs/relatorios/onda1-carga-YYYY-MM-DD.json`.
