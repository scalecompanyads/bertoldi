# Onda 1 — acessibilidade (axe / Playwright)

Data: 2026-07-22  
Responsável: Engenharia

## Resultado

**Suite entregue.** O teste público de login passou em 2026-07-22 (Chromium light, zero
violações critical/serious). Execução autenticada permanece bloqueada sem credenciais E2E.

- Spec: `tests/e2e/accessibility.spec.ts`
- Config: `playwright.config.ts` (light + dark)
- Correções já no produto: skip link, `aria-current`, `role="alert"`, `error.tsx` /
  `loading.tsx`, labels na busca, botões de mover coluna no Kanban

## Como executar

```bash
# Login público (sem credenciais)
npx playwright test tests/e2e/accessibility.spec.ts -g "login"

# Rotas autenticadas
E2E_ADMIN_EMAIL=... E2E_ADMIN_PASSWORD=... \
E2E_CLIENT_EMAIL=... E2E_CLIENT_PASSWORD=... \
npm run test:e2e
```

## Meta

Zero violações `critical` ou `serious` (WCAG 2.1 A/AA) nas rotas:
`/login`, `/admin`, `/admin/clientes`, `/admin/processos`, `/admin/importar`,
`/admin/calendarios`, `/cliente`, `/cliente/comunicados`, `/cliente/perfil`.
